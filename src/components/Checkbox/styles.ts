import { StyleSheet } from "react-native";

const checkbox = StyleSheet.create({
  checkboxContainer: { flexDirection: "row", marginTop: 10 },
  checkboxInput: { marginTop: 8 }
});

export default checkbox;
