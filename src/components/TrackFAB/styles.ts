import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  mainContainer: { flexDirection: "row", marginTop: 10 },
  finishButtonContainer: { padding: 17 },
  trackButtonContainer: { marginLeft: 10 }
});

export default styles;
