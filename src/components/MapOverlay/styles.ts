import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  overlayContainer: { zIndex: 20, position: "absolute", bottom: 20, right: 15 },
  buttonContainer: { alignItems: "flex-end" }
});

export default styles;
