import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleContainer: { paddingTop: 20, paddingHorizontal: 20 },
  backArrowContainer: { paddingVertical: 20, flexDirection: "row" }
});

export default styles;
