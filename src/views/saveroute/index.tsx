import { connect } from "react-redux";

import Component from "./Component";

const mapStateToProps = () => ({});

const mapDispatchToProps = () => ({});

const saveRouteContainer = connect(
  mapStateToProps,
  mapDispatchToProps
)(Component);

export default saveRouteContainer;
