import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleContainer: { paddingTop: 20, paddingHorizontal: 20 },
  contentContainer: { padding: 20 },
  itemSeperator: { margin: 10 }
});

export default styles;
